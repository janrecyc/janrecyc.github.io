/* ============================================================
   data/countries.js — server/country list shown on country.html.
   Empty for now. When ready, add objects like:
     { flag: '🇺🇸', name: 'สหรัฐอเมริกา', ping: '28 ms' }
   and components/country-list.js will render them — no HTML
   changes needed on country.html.
   ============================================================ */
const COUNTRY_ITEMS = [];
