// ══════════════════════════════════════════
//  supabase.js — เดิมไว้ตั้งค่า Supabase
//  ตอนนี้แอพนี้ใช้ SQLite ในเครื่องแทนทั้งหมด (local-rest.js)
//  เก็บตัวแปรเหล่านี้ไว้เพื่อไม่ให้โค้ดเดิมที่อ้างอิงชื่อนี้พัง
// ══════════════════════════════════════════

const SUPABASE_URL  = 'local://offline';
const SUPABASE_ANON = 'local';
const SUPABASE_READY = true; // ให้ทุกหน้าใช้เส้นทาง "จริง" (บันทึกลง SQLite) ไม่ใช่โหมด Demo
